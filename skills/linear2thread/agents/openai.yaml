interface:
  display_name: "Linear to T3 Thread"
  short_description: "Shared RPC dispatch into native T3 worktrees"
  default_prompt: "Use $linear2thread to open selected Linear issues in native T3 worktree threads with the requested Claude or Codex profile."
