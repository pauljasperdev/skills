interface:
  display_name: "Implement"
  short_description: "Build minimal, maintainable, verified implementations"
  default_prompt: "Use $implement to deliver only the requested behavior with necessary, minimal code; ask about unclear requirements before implementing them and verify the result."
