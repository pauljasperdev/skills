interface:
  display_name: "Release"
  short_description: "Prepare a versioned release PR with release notes"
  default_prompt: "Use $release to prepare the patch version bump and a release PR to main explaining the actual changes, deployment requirements, and validation."

policy:
  allow_implicit_invocation: false
