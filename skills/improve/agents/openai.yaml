interface:
  display_name: "Improve"
  short_description: "Refine a completed implementation"
  default_prompt: "Use $improve to review the implementation just completed, make worthwhile improvements, and verify the result."

policy:
  allow_implicit_invocation: false
