interface:
  display_name: "Linear to Claude"
  short_description: "Open Linear issues in Fable worktrees"
  default_prompt: "Use $linear2claude with the shared $linear2thread RPC workflow to open unblocked Linear issues in Claude Fable 5.1/high T3 worktree threads."

policy:
  allow_implicit_invocation: false
